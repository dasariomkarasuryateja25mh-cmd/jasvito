# GramServe

Local services marketplace built with Next.js, React, TypeScript and Supabase.

## Run locally

Keep your existing `.env.local` in the project root. Then run `npm install`, `npm run build`, and `npm run start`.

Existing authentication intentionally continues to use the internal `@jasvito.local` address format so existing accounts remain compatible.
